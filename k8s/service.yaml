apiVersion: v1
kind: Service
metadata:
  name: cloudnative-go-lab
  labels:
    app: cloudnative-go-lab
spec:
  type: ClusterIP
  selector:
    app: cloudnative-go-lab
  ports:
    - name: http
      port: 80
      targetPort: 8080
